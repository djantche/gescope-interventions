<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>{% block title %}GeScope{% endblock %}</title>
  <link rel="stylesheet" href="{{ asset('css/style.css') }}">
</head>
<body>
  <header class="site-header">
    <div class="container">
      <h1><a href="{{ path('intervention_index') }}">GeScope — Interventions</a></h1>
    </div>
  </header>
  <main class="container">
    {% for type, messages in app.flashes %}
      {% for msg in messages %}
        <div class="flash {{ type }}">{{ msg }}</div>
      {% endfor %}
    {% endfor %}
    {% block body %}{% endblock %}
  </main>
  <script src="{{ asset('js/app.js') }}"></script>
</body>
</html>
